package com.example.myapplication.data.repository

import javax.inject.Inject
import javax.inject.Singleton

/**
 * --- ARCHIVO OBSOLETO (MAVERICK V5) ---
 * Este repositorio pertenecía al sistema de login manual antiguo.
 * Actualmente se utiliza [AuthRepository] como fuente de verdad para Firebase Auth.
 */
@Singleton
class LoginRepository @Inject constructor() {
    // Código desactivado para evitar conflictos con el flujo de Firebase
}
